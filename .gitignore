# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# sitemap gerado (next-sitemap regenera no postbuild)
/public/sitemap*.xml

# env
.env
.env*.local

# rede de segurança do formulário 4-Week Experience: candidaturas que não
# puderam ser enviadas por e-mail, salvas em disco pra recuperação manual.
# Contém dados pessoais de candidatos reais — nunca deve ir pro controle de
# versão (ver lib/application-failsafe.ts).
/data/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# os
.DS_Store
Thumbs.db

# local Claude Code settings (machine-specific, not shared)
.claude/settings.local.json

# typescript
*.tsbuildinfo
next-env.d.ts
